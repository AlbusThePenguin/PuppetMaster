package me.albusthepenguin.api; // Ensure it's in the right package

public class APIExample {
    // A simple static variable we want to access in the core module
    public static String exampleMessage = "Hello from the API module!";
}